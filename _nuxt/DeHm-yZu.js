const e=JSON.parse(`[{"slug":"mangadb","title":"MangaDB","subtitle":"CASE STUDY // 001","description":"A fullstack manga collection manager that lets fans catalog their libraries, discover new titles, and track upcoming releases. Built with Vue.js on the frontend, Node.js/Express on the backend, and MySQL for scalable data persistence.\\n\\n## The Problem\\n\\nManga collectors lacked a centralized tool to manage their growing libraries across languages and editions. Existing solutions offered no multi-language support, no relationship mapping between series (spin-offs, sequels, prequels), and no way to preview physical volumes before purchasing — leaving fans fragmented across spreadsheets and scattered wikis.\\n\\n## The Solution\\n\\nBuilt a fullstack application with intelligent multi-language content resolution: the system first attempts the user's preferred language, falls back to an optional secondary edition (e.g. English covers or descriptions), and ultimately defaults to the origin language. Series pages aggregate descriptions, contributor credits, publisher data, and complete volume listings with cross-series relationship mapping for spin-offs, sequels, and prequels.\\n\\nA standout feature is the 3D volume previewer - when dimensions and spine/back cover imagery are available, users can inspect volumes in an interactive three-dimensional view powered by Three.js. The homepage surfaces trending data including the top 10 most-searched series of the month, recently added entries, and upcoming release dates, giving collectors a living dashboard for the manga ecosystem.","image":"/images/projects/mangadb-legacy.jpg","tags":["NUXT","Express","MySQL","Three.js"],"icon":"material-symbols:scatter-plot","stats":[{"label":"DB_ROWS","value":"300K"},{"label":"OLDEST_ENTRY","value":"1970"},{"label":"LINES_OF_CODE","value":"50K"}],"codeSnippets":[],"links":[{"label":"MangaDB","url":"https://manga-db.org"},{"label":"Status","url":"https://status.manga-db.org"}],"logo":"/images/projects/mangadb.jpg"},{"slug":"color-by-number","title":"Color by Number","subtitle":"CASE STUDY // 002","description":"A client-side color-by-number puzzle app built with Nuxt 3. Upload any image, convert it into a numbered puzzle, and solve it in the browser - no server required. All image processing runs entirely in TypeScript using K-Means color quantization in CIELAB color space, connected-component labeling, and marching squares contour extraction.\\n\\n## The Problem\\n\\nColor-by-number apps typically rely on server-side processing or pre-built puzzle libraries. They either require uploading images to a remote service, limit customization, or ship bloated runtimes. There was no lightweight, offline-capable solution that lets users bring their own images and tune every parameter of the conversion pipeline.\\n\\n## The Solution\\n\\nA fully client-side processing engine that converts arbitrary images into playable puzzles through a six-stage pipeline: downscale and blur preprocessing, K-Means++ clustering in perceptual color space, union-find region detection, small-region merging by shared border length, marching squares contour extraction with Douglas-Peucker simplification, and pole-of-inaccessibility label placement. Users control the number of colors, minimum region size, resolution, blur strength, and contour detail.\\n\\nThe interactive player supports concurrent fill animations, scroll/pinch zoom and pan, and a custom crosshair cursor for precision. Puzzles persist to IndexedDB for full offline support, with per-region progress tracking and a gallery with thumbnails and progress bars. Canvas redraws use cached ImageBitmap layers and Web Worker offloading — only affected regions re-render, keeping fill animations smooth at 60fps even on large puzzles.","image":"/images/projects/color-by-number.png","tags":["TYPESCRIPT","PRIMEVUE","INDEXEDDB","WEB WORKERS","K-MEANS","IMAGE PROCESSING"],"icon":"ic:outline-color-lens","stats":[{"label":"PIPELINE","value":"7 STAGES"},{"label":"COLORS","value":"UP TO 60"},{"label":"PROCESSING","value":"CLIENT-SIDE"},{"label":"FPS","value":"60+"}],"codeSnippets":[{"file":"quantize.ts","lang":"TypeScript","code":"/**\\n * Color Quantization — K-Means clustering in CIELAB color space.\\n *\\n * Reduces an image to K representative colors by:\\n * 1. Converting all pixels from RGB to perceptually uniform CIELAB space\\n * 2. Running K-Means++ (smart initialization) to find K cluster centroids\\n * 3. Assigning each pixel to its nearest centroid\\n * 4. Removing unused clusters and re-indexing\\n *\\n * The result is a compact palette and a per-pixel index grid.\\n */\\nimport type { RGBColor, LabColor, ProgressCallback } from './types'\\n\\n// ---- Color space conversion (RGB ↔ CIELAB via XYZ with D65 illuminant) ----\\n\\nexport function rgbToLab(c: RGBColor): LabColor {\\n  // RGB -> linear sRGB\\n  let r = c.r / 255\\n  let g = c.g / 255\\n  let b = c.b / 255\\n\\n  r = r > 0.04045 ? ((r + 0.055) / 1.055) ** 2.4 : r / 12.92\\n  g = g > 0.04045 ? ((g + 0.055) / 1.055) ** 2.4 : g / 12.92\\n  b = b > 0.04045 ? ((b + 0.055) / 1.055) ** 2.4 : b / 12.92\\n\\n  // linear sRGB -> XYZ (D65)\\n  let x = (r * 0.4124564 + g * 0.3575761 + b * 0.1804375) / 0.95047\\n  let y = (r * 0.2126729 + g * 0.7151522 + b * 0.0721750)\\n  let z = (r * 0.0193339 + g * 0.1191920 + b * 0.9503041) / 1.08883\\n\\n  const eps = 0.008856\\n  const kappa = 903.3\\n\\n  x = x > eps ? x ** (1 / 3) : (kappa * x + 16) / 116\\n  y = y > eps ? y ** (1 / 3) : (kappa * y + 16) / 116\\n  z = z > eps ? z ** (1 / 3) : (kappa * z + 16) / 116\\n\\n  return {\\n    L: 116 * y - 16,\\n    a: 500 * (x - y),\\n    b: 200 * (y - z),\\n  }\\n}\\n\\nexport function labToRgb(lab: LabColor): RGBColor {\\n  const y = (lab.L + 16) / 116\\n  const x = lab.a / 500 + y\\n  const z = y - lab.b / 200\\n\\n  const eps = 0.008856\\n  const kappa = 903.3\\n\\n  const x3 = x ** 3\\n  const z3 = z ** 3\\n  const y3 = y ** 3\\n\\n  const xr = x3 > eps ? x3 : (116 * x - 16) / kappa\\n  const yr = lab.L > kappa * eps ? y3 : lab.L / kappa\\n  const zr = z3 > eps ? z3 : (116 * z - 16) / kappa\\n\\n  // XYZ -> linear sRGB\\n  const lr = xr * 0.95047\\n  const mg = yr * 1.0\\n  const nr = zr * 1.08883\\n\\n  let r = lr * 3.2404542 + mg * -1.5371385 + nr * -0.4985314\\n  let g = lr * -0.9692660 + mg * 1.8760108 + nr * 0.0415560\\n  let b = lr * 0.0556434 + mg * -0.2040259 + nr * 1.0572252\\n\\n  // Gamma\\n  r = r > 0.0031308 ? 1.055 * r ** (1 / 2.4) - 0.055 : 12.92 * r\\n  g = g > 0.0031308 ? 1.055 * g ** (1 / 2.4) - 0.055 : 12.92 * g\\n  b = b > 0.0031308 ? 1.055 * b ** (1 / 2.4) - 0.055 : 12.92 * b\\n\\n  return {\\n    r: Math.max(0, Math.min(255, Math.round(r * 255))),\\n    g: Math.max(0, Math.min(255, Math.round(g * 255))),\\n    b: Math.max(0, Math.min(255, Math.round(b * 255))),\\n  }\\n}\\n\\nfunction labDistSq(a: LabColor, b: LabColor): number {\\n  const dL = a.L - b.L\\n  const da = a.a - b.a\\n  const db = a.b - b.b\\n  return dL * dL + da * da + db * db\\n}\\n\\n// ---- K-Means++ initialization ----\\n// Selects initial centroids that are well-spread, leading to faster convergence\\n// than random initialization. Each successive centroid is chosen with probability\\n// proportional to its squared distance from the nearest existing centroid.\\n\\nfunction initKMeansPP(pixels: LabColor[], k: number): LabColor[] {\\n  const centroids: LabColor[] = []\\n  const n = pixels.length\\n\\n  // Pick first centroid randomly\\n  centroids.push({ ...pixels[Math.floor(Math.random() * n)] })\\n\\n  const dists = new Float64Array(n).fill(Infinity)\\n\\n  for (let c = 1; c < k; c++) {\\n    const last = centroids[c - 1]\\n    let totalDist = 0\\n    for (let i = 0; i < n; i++) {\\n      const d = labDistSq(pixels[i], last)\\n      if (d < dists[i]) dists[i] = d\\n      totalDist += dists[i]\\n    }\\n\\n    // Weighted random selection\\n    let target = Math.random() * totalDist\\n    let idx = 0\\n    for (let i = 0; i < n; i++) {\\n      target -= dists[i]\\n      if (target <= 0) { idx = i; break }\\n    }\\n    centroids.push({ ...pixels[idx] })\\n  }\\n\\n  return centroids\\n}\\n\\n/** Yields to main thread periodically */\\nfunction yieldToMain(): Promise<void> {\\n  return new Promise(resolve => setTimeout(resolve, 0))\\n}\\n\\n/**\\n * Main quantization entry point.\\n * Converts pixels to Lab, runs K-Means clustering, and returns:\\n * - palette: K (or fewer) representative RGB colors\\n * - indexGrid: flat array mapping each pixel to its palette index\\n *\\n * Yields to the main thread periodically to keep the UI responsive.\\n */\\nexport async function quantize(\\n  imageData: ImageData,\\n  k: number,\\n  onProgress?: ProgressCallback,\\n): Promise<{ palette: RGBColor[]; indexGrid: Uint16Array }> {\\n  const { data, width, height } = imageData\\n  const totalPixels = width * height\\n\\n  onProgress?.('Converting colors…', 0)\\n\\n  // Convert all pixels to Lab\\n  const pixels: LabColor[] = new Array(totalPixels)\\n  for (let i = 0; i < totalPixels; i++) {\\n    const off = i * 4\\n    pixels[i] = rgbToLab({ r: data[off], g: data[off + 1], b: data[off + 2] })\\n    if (i % 50000 === 0) {\\n      onProgress?.('Converting colors…', (i / totalPixels) * 30)\\n      await yieldToMain()\\n    }\\n  }\\n\\n  onProgress?.('Analyzing colors…', 30)\\n\\n  // K-Means++ init\\n  let centroids = initKMeansPP(pixels, k)\\n  const assignments = new Uint16Array(totalPixels)\\n\\n  const maxIter = 20\\n  for (let iter = 0; iter < maxIter; iter++) {\\n    onProgress?.('Analyzing colors…', 30 + (iter / maxIter) * 60)\\n\\n    // Assignment step\\n    let changed = 0\\n    for (let i = 0; i < totalPixels; i++) {\\n      let bestDist = Infinity\\n      let bestIdx = 0\\n      for (let c = 0; c < k; c++) {\\n        const d = labDistSq(pixels[i], centroids[c])\\n        if (d < bestDist) { bestDist = d; bestIdx = c }\\n      }\\n      if (assignments[i] !== bestIdx) { changed++ }\\n      assignments[i] = bestIdx\\n    }\\n\\n    // Check convergence\\n    if (changed === 0) break\\n\\n    // Update step\\n    const sums: LabColor[] = Array.from({ length: k }, () => ({ L: 0, a: 0, b: 0 }))\\n    const counts = new Uint32Array(k)\\n    for (let i = 0; i < totalPixels; i++) {\\n      const c = assignments[i]\\n      sums[c].L += pixels[i].L\\n      sums[c].a += pixels[i].a\\n      sums[c].b += pixels[i].b\\n      counts[c]++\\n    }\\n\\n    for (let c = 0; c < k; c++) {\\n      if (counts[c] > 0) {\\n        centroids[c] = {\\n          L: sums[c].L / counts[c],\\n          a: sums[c].a / counts[c],\\n          b: sums[c].b / counts[c],\\n        }\\n      }\\n    }\\n\\n    await yieldToMain()\\n  }\\n\\n  onProgress?.('Building palette…', 90)\\n\\n  // Convert centroids back to RGB\\n  const palette = centroids.map(c => labToRgb(c))\\n\\n  // Handle case where some clusters are empty — reassign to nearest non-empty\\n  const usedIndices = new Set<number>()\\n  for (let i = 0; i < totalPixels; i++) usedIndices.add(assignments[i])\\n\\n  const finalPalette: RGBColor[] = []\\n  const indexRemap = new Map<number, number>()\\n  let newIdx = 0\\n  for (let c = 0; c < k; c++) {\\n    if (usedIndices.has(c)) {\\n      indexRemap.set(c, newIdx)\\n      finalPalette.push(palette[c])\\n      newIdx++\\n    }\\n  }\\n\\n  const indexGrid = new Uint16Array(totalPixels)\\n  for (let i = 0; i < totalPixels; i++) {\\n    indexGrid[i] = indexRemap.get(assignments[i]) ?? 0\\n  }\\n\\n  onProgress?.('Palette complete', 100)\\n  return { palette: finalPalette, indexGrid }\\n}"}],"links":[{"label":"Website","url":"https://color-by-number.bluetvcoding.workers.dev/"}]},{"slug":"lego-builder","title":"Lego Builder","subtitle":"CASE STUDY // 003","description":"A browser-based 3D LEGO builder powered by Babylon.js. Place, color, move, and snap bricks on an interactive grid - entirely client-side with no backend dependencies. Features a full editor UI with toolbox, inspector panel, color picker, file save/load, and collision detection.\\n\\n## The Problem\\n\\nExisting LEGO building tools are either desktop-only applications with heavy installs, or simplified 2D web toys that lack real spatial building mechanics. There was no lightweight, browser-native 3D builder that offers a desktop-app-like editing experience with proper grid snapping, brick stacking, collision feedback, and a rich color system - all running without a server.\\n\\n## The Solution\\n\\nBuilt a complete 3D editing environment using Babylon.js with ArcRotate camera controls, grid-snapped brick placement, and AABB collision detection that highlights invalid positions in real time. Bricks are loaded as glTF models through an asset manager pipeline and rendered with flat shading for the characteristic LEGO aesthetic.\\n\\nThe editor supports multi-select (Ctrl+click), copy/paste, select-all, and delete operations with full keyboard shortcuts. A categorized toolbox (Bricks, Plates, Tiles, Arches, Slopes, Panels, Antennas, Containers, Deco) with search lets users browse the brick library visually. The inspector panel tracks all placed elements and provides a color picker with 60+ authentic LEGO colors organized by category. Builds can be saved to and loaded from .brick files (JSON format), and the rendering pipeline includes configurable FXAA, sharpening, and HDR support. Brick placement triggers randomized snap sounds for tactile feedback.","image":"/images/projects/prisma-ui.jpg","tags":["BABYLON.JS","3D","GLTF","JAVASCRIPT","CANVAS","COLLISION DETECTION"],"icon":"material-symbols:grid-view","stats":[{"label":"BRICK TYPES","value":"15"},{"label":"COLORS","value":"60+"},{"label":"CATEGORIES","value":"9"},{"label":"BACKEND","value":"NONE"}],"codeSnippets":[{"file":"editor.js - Brick instatiation","lang":"JavaScript","code":"addBrickByName(name, position, color) {\\n    const brickData = BrickLib.bricks.find(b => b.name === name);\\n    const brickMesh = BrickLib.brickMeshes.get(name);\\n    const brick = brickMesh.clone(name);\\n    brick.isPickable = true;\\n    brick.position = position || new BABYLON.Vector3(0, 0, 0).add(brickData.offset);\\n\\n    // Create material with RGBA color conversion (Babylon uses 0-1 range)\\n    const brickMaterial = new BABYLON.StandardMaterial(\\"mat\\", this.scene);\\n    const r = (color === undefined ? this.currentColor.rgba[0] / 255 : color.rgba[0] / 255);\\n    const g = (color === undefined ? this.currentColor.rgba[1] / 255 : color.rgba[1] / 255);\\n    const b = (color === undefined ? this.currentColor.rgba[2] / 255 : color.rgba[2] / 255);\\n    brickMaterial.diffuseColor = new BABYLON.Color3(r, g, b);\\n    brickMaterial.alpha = (color === undefined ? this.currentColor.rgba[3] : color.rgba[3]);\\n    brickMaterial.specularColor = new BABYLON.Color3(0.2, 0.2, 0.2);\\n    brickMaterial.emissiveColor = new BABYLON.Color3(0, 0, 0);\\n    brickMaterial.ambientColor = new BABYLON.Color3(0.75, 0.75, 0.75);\\n    brick.material = brickMaterial;\\n\\n    if (!brick) return;\\n\\n    // Set brick offset for grid snapping\\n    brick.offset = brickData.offset || new BABYLON.Vector3(0, 0, 0);\\n\\n    // Build custom bounding box for collision detection\\n    const boundingBoxOffset = 0.01;\\n    const _bbOff = brickData.boundingBoxOffset || new BABYLON.Vector3(0, 0, 0);\\n    const brickMin = new BABYLON.Vector3(\\n        -(0.1 * brickData.width) + boundingBoxOffset + _bbOff.x,\\n        this.grid.position._y + _bbOff.y,\\n        -(0.1 * brickData.depth) + boundingBoxOffset + _bbOff.z\\n    );\\n    const brickMax = new BABYLON.Vector3(\\n        (0.1 * brickData.width) - boundingBoxOffset + _bbOff.x,\\n        this.grid.position._y + (brickData.height - boundingBoxOffset) + _bbOff.y,\\n        (0.1 * brickData.depth) - boundingBoxOffset + _bbOff.z\\n    );\\n    brick._boundingInfo.boundingBox = new BABYLON.BoundingBox(\\n        brickMin, brickMax, brick._boundingInfo.boundingBox._worldMatrix\\n    );\\n\\n    // Register in editor element list\\n    this.elements.push({\\n        id: brick.uniqueId,\\n        name,\\n        element: brick,\\n        data: brickData,\\n        type: 'brick',\\n        selected: false,\\n        hovering: false,\\n        edgeRendererEnabled: false,\\n        invalidPos: false,\\n        color: (color === undefined ? this.currentColor : color),\\n    });\\n\\n    document.dispatchEvent(new CustomEvent('brick-added', {\\n        detail: { id: brick.uniqueId, name, position, color }\\n    }));\\n}"},{"file":"editor.js - Grid snapping","lang":"JavaScript","code":"this.scene.onPointerMove = (evt) => {\\n    // If no brick is picked, just highlight hovered bricks\\n    if (!this.currentMesh) {\\n        this.highlightHoveredMesh();\\n        return;\\n    }\\n\\n    // Get grid-aligned position from raycast\\n    const grid = this.getGridPosition(evt);\\n    if (!grid) return;\\n\\n    // Snap to grid (0.2 unit intervals) with per-brick offset\\n    const pos = new BABYLON.Vector3(\\n        Math.round(grid.x / 0.2) * 0.2 + this.currentMesh.offset.x,\\n        grid._y + this.currentMesh.offset.y,\\n        Math.round(grid.z / 0.2) * 0.2 + this.currentMesh.offset.z\\n    );\\n\\n    // Move brick and compute delta for multi-select group movement\\n    const previousPosition = this.currentMesh.position;\\n    this.currentMesh.position = pos;\\n    const posDiff = this.currentMesh.position.subtract(previousPosition);\\n\\n    // Apply same delta to all other selected bricks\\n    const selectedElements = this.elements.filter(\\n        el => el.selected === true && el.element !== this.currentMesh && el.type === 'brick'\\n    );\\n    selectedElements.forEach(el => {\\n        // Some elements use _x/_y/_z instead of x/y/z\\n        try {\\n            el.element.position = el.element.position.add(posDiff);\\n        } catch (e) {\\n            let pos = new BABYLON.Vector3(\\n                el.element.position._x || el.element.position.x,\\n                el.element.position._y || el.element.position.y,\\n                el.element.position._z || el.element.position.z\\n            );\\n            pos = pos.add(posDiff);\\n            el.element.position = pos;\\n        }\\n    });\\n\\n    // Check for overlapping bricks after movement\\n    this.checkForCollisions();\\n};"}]},{"slug":"picross-web","title":"Picross Web","subtitle":"CASE STUDY // 004","description":"A browser-based clone of Nintendo's Picross 3D puzzle game, built with Three.js. Chip away at a 3D grid of cubes using number hints to reveal a hidden shape - with layer slicing, color reveals, star ratings, and a custom level file format.\\n\\n## The Problem\\n\\nPicross 3D is a beloved Nintendo DS puzzle game with no official web version. Existing nonogram/picross clones are almost exclusively 2D, ignoring the spatial reasoning that makes the 3D variant compelling. Players had no way to experience this style of puzzle in a browser without emulation.\\n\\n## The Solution\\n\\nBuilt a complete 3D puzzle engine using Three.js with orbit camera controls, raycasting-based block interaction, and a custom .lvl file format that encodes grid dimensions, block states, and color data per layer. The game parses level files line-by-line - each character represents a block state (destroyable vs. non-destroyable), with optional color values appended for the final reveal.\\n\\nHint calculation runs automatically at load time: for every block position, the engine counts non-destroyable blocks along all three axes (X, Y, Z) and tracks how many contiguous groups they form - rendering the appropriate single, double, or triple hint markers on each cube face. Players right-click to mark blocks that should remain and Alt+right-click to destroy blocks, with a strike system for incorrect breaks. Draggable layer handles let players slice through the puzzle on any axis to inspect hidden blocks. Completed rows/columns auto-dim to signal progress, and solving the puzzle triggers a color reveal animation that transitions each block to its final color with staggered timing. Progress and star ratings (based on completion time) persist to localStorage.","image":"/images/projects/neural-link.jpg","tags":["THREE.JS","3D","JAVASCRIPT","RAYCASTING","PUZZLE","CANVAS"],"icon":"material-symbols:auto-awesome","stats":[{"label":"LEVELS","value":"6"},{"label":"AXES","value":"3 (X/Y/Z)"},{"label":"HINT TYPES","value":"3"}],"codeSnippets":[{"file":"app.js - Raycasting","lang":"JavaScript","code":"// Mouse down handler — block destruction and marking via raycasting\\nmouseDown(e) {\\n    e.preventDefault();\\n    if (e.button === 0 || this.pauseMenu.classList.contains('show')) return;\\n\\n    // Cast ray from mouse position into 3D scene\\n    const mouse3D = Util.pointToVector(e.clientX, e.clientY);\\n    const raycaster = new THREE.Raycaster();\\n    raycaster.setFromCamera(mouse3D, this.camera);\\n    const intersects = raycaster.intersectObjects(\\n        this.scene.children.filter(c => !c.disableRaycast)\\n    );\\n\\n    if (intersects.length > 0) {\\n        const obj = intersects[0].object;\\n        const x = Math.floor(obj.position.x);\\n        const y = Math.floor(obj.position.y);\\n        const z = Math.floor(obj.position.z);\\n\\n        // Alt + Right Click: destroy block\\n        if (e.button === 2 && e.altKey) {\\n            const blockState = this.level.blockMap[x][y][z];\\n\\n            // Marked blocks can't be destroyed\\n            if (obj.marked === true) {\\n                this.transitions.push(new Transition(obj, 'scale', 1.1, 1.0, 'in'));\\n                return;\\n            }\\n\\n            // Wrong guess — block is part of the solution\\n            if (blockState === BLOCK_STATE.NON_DESTROYABLE) {\\n                obj.marked = true;\\n                this.transitions.push(new Transition(obj, 'scale', 1.1, 1.0, 'in'));\\n                for (const mat of obj.material) mat.color.setHex(0x1c55a0);\\n                this.checkForRowsColumns(x, y, z);\\n                return;\\n            }\\n\\n            // Correct guess — animate out and dispose\\n            this.transitions.push(new Transition(obj, 'scale', 0.0, 1.0, 'out', 0.02));\\n            obj.disableRaycast = true;\\n            this.level.blocks[x][y][z] = undefined;\\n            this.checkForRowsColumns(x, y, z);\\n            setTimeout(() => {\\n                obj.geometry.dispose();\\n                obj.material.forEach(mat => mat.dispose());\\n                this.scene.remove(obj);\\n            }, 400);\\n\\n        // Right Click: toggle mark\\n        } else if (e.button === 2) {\\n            this.transitions.push(new Transition(obj, 'scale', 1.1, 1.0, 'in'));\\n            if (obj.marked === true && obj.fixed === true) return;\\n            obj.marked = !obj.marked;\\n            for (const mat of obj.material) {\\n                mat.color.setHex(obj.marked ? 0x1c55a0 : 0xFFFFFF);\\n            }\\n            this.checkForRowsColumns(x, y, z);\\n        }\\n    }\\n}"},{"file":"level.js - Hint calculation","lang":"JavaScript","code":"static loadFromFile(path) {\\n  return new Promise((resolve, reject) => {\\n    fetch(path).then((res) => res.text())\\n    .then((text) => {\\n      // Read line by line\\n      let lines = text.split('\\\\n').map((l) => l.replaceAll('\\\\r', ''));\\n      // First line is the size of the level (x y z)\\n      const size = lines[0].split(' ').map((val) => parseInt(val));\\n      const level = new Level(path, size);\\n\\n      // Parse each line and set block states\\n      let index = 1;\\n      let layer = 0;\\n      while (index < lines.length) {\\n        if (lines[index].includes('1')) {\\n          const blocks = lines[index].split(' ')[0];\\n          const colorLine = lines[index].split(' ')[1];\\n          const colors = colorLine.includes(';')\\n            ? colorLine.split(';') : [colorLine];\\n          let colorIndex = 0;\\n\\n          for (let c = 0; c < blocks.length; c++) {\\n            const blockVal = parseInt(blocks[c]);\\n            level.blockMap[c][layer][(index - 1) % level.size.z] = blockVal;\\n\\n            if (blockVal === 1) {\\n              level.colorMap[c][layer][(index - 1) % level.size.z] =\\n                colors[colorIndex];\\n              colorIndex++;\\n            }\\n          }\\n        } else {\\n          for (let c = 0; c < lines[index].length; c++) {\\n            level.blockMap[c][layer][(index - 1) % level.size.z] =\\n              parseInt(lines[index][c]);\\n          }\\n        }\\n        index++;\\n        if ((index - 1) % level.size.z === 0) layer++;\\n      }\\n\\n      // Calculate hints for each block along all 3 axes\\n      for (let x = 0; x < level.size.x; x++) {\\n        for (let y = 0; y < level.size.y; y++) {\\n          for (let z = 0; z < level.size.z; z++) {\\n            let vertical = 0, verticalGroups = 0, inGroup = false;\\n\\n            for (let y1 = 0; y1 < level.size.y; y1++) {\\n              if (level.blockMap[x][y1][z] === BLOCK_STATE.NON_DESTROYABLE) {\\n                vertical++;\\n                if (!inGroup) { inGroup = true; verticalGroups++; }\\n              } else { inGroup = false; }\\n            }\\n\\n            inGroup = false;\\n            let hRL = 0, hRLGroups = 0;\\n            for (let x1 = 0; x1 < level.size.x; x1++) {\\n              if (level.blockMap[x1][y][z] === BLOCK_STATE.NON_DESTROYABLE) {\\n                hRL++;\\n                if (!inGroup) { inGroup = true; hRLGroups++; }\\n              } else { inGroup = false; }\\n            }\\n\\n            inGroup = false;\\n            let hBF = 0, hBFGroups = 0;\\n            for (let z1 = 0; z1 < level.size.z; z1++) {\\n              if (level.blockMap[x][y][z1] === BLOCK_STATE.NON_DESTROYABLE) {\\n                hBF++;\\n                if (!inGroup) { inGroup = true; hBFGroups++; }\\n              } else { inGroup = false; }\\n            }\\n\\n            // Map group count to hint type\\n            const type = (g) =>\\n              g <= 1 ? 'single' : g === 2 ? 'two' : 'three';\\n\\n            level.hintMap[x][y][z] = {\\n              vertical,\\n              verticalType: type(verticalGroups),\\n              horizontalBackFront: hBF,\\n              horizontalBackFrontType: type(hBFGroups),\\n              horizontalRightLeft: hRL,\\n              horizontalRightLeftType: type(hRLGroups),\\n            };\\n          }\\n        }\\n      }\\n      resolve(level);\\n    }).catch((e) => console.error(e));\\n  });\\n}"}],"links":[{"label":"Demo","url":"/projects/picross-3d"}]}]`),n={projects:e};export{n as p};
